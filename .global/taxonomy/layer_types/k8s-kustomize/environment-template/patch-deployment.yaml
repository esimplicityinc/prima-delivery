apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ layer_name }}
spec:
  template:
    spec:
      containers:
        - name: {{ layer_name }}
          image: nginx:latest
          env:
            - name: ENVIRONMENT
              value: {{ environment }}
