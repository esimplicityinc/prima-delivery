apiVersion: katalyst.taxonomy/v1alpha
kind: TaxonomyNode
metadata:
  name: "{{ name }}"
taxonomyNodeType: program
spec:
  description: "{{ description }}"
  owners:
    - "{{ primary_owner }}"
  parents:
    node: "{{ parent }}"
  environments:
    - "{{ primary_environment }}"
