apiVersion: katalyst.taxonomy/v1alpha
kind: TaxonomyNode
metadata:
  name: "{{ name }}"
taxonomyNodeType: team
spec:
  description: "{{ description }}"
  owners:
    - "{{ primary_owner }}"
  parents:
    node: "{{ parent }}"
  environments:
    - "{{ primary_environment }}"
  team:
    unitType: squad
