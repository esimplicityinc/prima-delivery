apiVersion: katalyst.taxonomy.plugins.actions/v1alpha
kind: Action
metadata:
  name: k8s-fmt
spec:
  description: Validate rendered Kubernetes manifests with kubeconform.
  type: shell
  when: pre
  run: just k8s-kustomize-fmt
  tags:
    - validate
    - schema
    - kubernetes
